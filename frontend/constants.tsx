const host = 'http://192.168.101.145';
const backendPort = '5000';
const apiVersion = 'api/v1';

export const apiUrl = `${host}:${backendPort}/${apiVersion}`;

export const workUrl = `${apiUrl}/work`;
